# AI-lead-automation-system

A production-grade pipeline that captures incoming leads via webhook, enriches company data, classifies intent using an LLM, stores structured records, and triggers real-time notifications. Built with FastAPI (Python) for the backend and n8n for workflow orchestration.
